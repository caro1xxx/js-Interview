1.如果两个项是同类型,那么==就会返回===操作符的执行结果
2.如果两个项不是同类型,==就会对其中一个或两个进行类型转换然后再比较.
整体流程
  1.如果类型相同,调用===操作符
  2.如果类型不同,尝试类型转换
      1.查看是否是undefined和null比较
          返回true
          如果不是继续下一条规则
      2.查看是否是string和number
          如果是,那么将string转为number并回到最初重新比较
          如果不是继续下一条规则
      3.查看比较项中是否有boolean
          如果有,那么将boolean转为number并回到最初重新比较
          如果不是继续下一条规则
      4.查看是否有一项是object
          如果有,那么将object转为其原始值primitive并回到最初重新比较
          如果还不是,只能返回false了