createElement() 创建
appendChild(childNode) 插入子节点
insertBefore(newNode, targetNode)插入新的节点
replace(newNode, targetNode) 替换
removeChild(childNode) 移除

node.setAttribute() 修改特性
node.classList.add() / remove() 添加/移除指定类
node.style.xxx 修改css样式

等等...